
paths:
  data:
    data_dir: "data"
    raw_dir: "data/raw"
    processed_dir: "data/processed"
    interim_dir: "data/interim"
  reports:
    reports_dir: "reports"
    plots_dir: "reports/plots"
  models: 
    e_models_dir: models/ecommerce
    c_models_dir: models/credit_card